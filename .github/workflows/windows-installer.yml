name: Windows installer

on:
  push:
    tags: ['v*']
  pull_request:
    paths:
      - 'packaging/windows/**'
      - 'local-client/build.gradle'
      - '.github/workflows/windows-installer.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  installer:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install WiX Toolset 3
        run: |
          choco install wixtoolset --no-progress -y
          "${env:ProgramFiles(x86)}\WiX Toolset v3.14\bin" |
            Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8

      - name: Build the installer
        run: .\packaging\windows\build-installer.ps1 -Jdk "$env:JAVA_HOME"

      - name: Install
        run: |
          $ErrorActionPreference = 'Stop'
          $msi = Get-ChildItem dest\windows\*.msi | Select-Object -First 1
          Write-Host "Installing $($msi.Name)"
          $p = Start-Process msiexec -ArgumentList @('/i', "`"$($msi.FullName)`"", '/qn', '/l*v', 'install.log') -Wait -PassThru
          if ($p.ExitCode -ne 0) {
              Get-Content install.log -Tail 60
              throw "msiexec exited with code $($p.ExitCode)"
          }

      - name: Check what was installed
        run: |
          $ErrorActionPreference = 'Stop'

          $exe = Join-Path $env:LOCALAPPDATA 'Ramus\Ramus.exe'
          if (-not (Test-Path $exe)) { throw "not found: $exe" }
          Write-Host "Executable: $exe"

          $menu = Join-Path $env:APPDATA 'Microsoft\Windows\Start Menu\Programs\Ramus'
          if (-not (Test-Path $menu)) { throw "no Start menu group: $menu" }
          Get-ChildItem $menu | ForEach-Object { Write-Host "Shortcut: $($_.Name)" }

          foreach ($ext in @('.ramus', '.rsf')) {
              $key = "HKCU:\Software\Classes\$ext"
              if (-not (Test-Path $key)) { throw "extension not registered: $ext" }
              Write-Host "$ext -> $((Get-ItemProperty $key).'(default)')"
          }

          $wi = New-Object -ComObject WindowsInstaller.Installer
          $code = $null
          foreach ($c in $wi.GetType().InvokeMember('Products', 'GetProperty', $null, $wi, $null)) {
              try {
                  $n = $wi.GetType().InvokeMember('ProductInfo', 'GetProperty',
                      $null, $wi, @($c, 'ProductName'))
              } catch { continue }
              if ($n -eq 'Ramus') { $code = $c; break }
          }
          if (-not $code) { throw 'Windows Installer does not know a product named Ramus' }

          $assignment = $wi.GetType().InvokeMember('ProductInfo', 'GetProperty',
              $null, $wi, @($code, 'AssignmentType'))
          $location = $wi.GetType().InvokeMember('ProductInfo', 'GetProperty',
              $null, $wi, @($code, 'InstallLocation'))
          Write-Host "Product: $code"
          Write-Host "AssignmentType: $assignment (0 = per-user, 1 = per-machine)"
          Write-Host "InstallLocation: $location"
          if ($assignment -ne '0') {
              throw "the install is not per-user (AssignmentType=$assignment): administrator rights would be needed, and the README promises otherwise"
          }

          foreach ($root in @(
                  'HKCU:\Software\Microsoft\Windows\CurrentVersion\Uninstall',
                  'HKLM:\Software\Microsoft\Windows\CurrentVersion\Uninstall',
                  'HKLM:\Software\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall')) {
              if (-not (Test-Path $root)) { continue }
              $row = Get-ChildItem $root |
                  ForEach-Object { Get-ItemProperty $_.PSPath } |
                  Where-Object { $_.DisplayName -eq 'Ramus' } |
                  Select-Object -First 1
              if ($row) { Write-Host "In the installed programs list ($root): $($row.DisplayName) $($row.DisplayVersion)" }
          }

      - name: Start the installed application
        run: |
          $ErrorActionPreference = 'Stop'
          $exe = Join-Path $env:LOCALAPPDATA 'Ramus\Ramus.exe'
          $app = Start-Process $exe -PassThru
          Start-Sleep -Seconds 30
          if ($app.HasExited) {
              throw "the application exited right after start, code $($app.ExitCode)"
          }
          Write-Host 'Still running after 30 s — the application starts'
          Stop-Process -Id $app.Id -Force

      - name: Uninstall
        run: |
          $ErrorActionPreference = 'Stop'
          $msi = Get-ChildItem dest\windows\*.msi | Select-Object -First 1
          $p = Start-Process msiexec -ArgumentList @('/x', "`"$($msi.FullName)`"", '/qn') -Wait -PassThru
          if ($p.ExitCode -ne 0) { throw "uninstall exited with code $($p.ExitCode)" }
          $dir = Join-Path $env:LOCALAPPDATA 'Ramus'
          if (Test-Path $dir) { throw "the directory survived the uninstall: $dir" }
          Write-Host 'Uninstalled cleanly'

      - uses: actions/upload-artifact@v4
        with:
          name: ramus-windows
          path: dest/windows/*
          if-no-files-found: error

      - name: Attach to the release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$env:GITHUB_REF_NAME" --generate-notes 2>$null
          gh release upload "$env:GITHUB_REF_NAME" (Get-ChildItem dest\windows\*).FullName --clobber
